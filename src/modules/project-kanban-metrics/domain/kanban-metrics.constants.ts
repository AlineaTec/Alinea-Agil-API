/** Semanas incluidas por defecto en throughput (ventana fija hacia atrás). */
export const KANBAN_METRICS_DEFAULT_THROUGHPUT_WEEKS = 12

/** Máximo rango `from`–`to` permitido (días). */
export const KANBAN_METRICS_MAX_RANGE_DAYS = 366

/** Cuántos días de auditoría cargar para replay por ítem (aging / lead). */
export const KANBAN_METRICS_AUDIT_LOOKBACK_DAYS = 450

/** Máximo de filas de aging devueltas. */
export const KANBAN_METRICS_AGING_TOP_N = 100
